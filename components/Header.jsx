const React = require('react');

function Header() {
  return (
    <header>
      <nav>
        <ul>
          <li>
            <a href="/">Вернуться к регистрации</a>
          </li>
        </ul>
      </nav>
    </header>
  );
}

module.exports = Header;
